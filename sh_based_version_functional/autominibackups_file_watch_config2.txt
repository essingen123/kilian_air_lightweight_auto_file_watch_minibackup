# Enter files to watch (one per line):
# Example: /path/to/file1.txt
# Example: below watching a file that does or does not exists (just jumps over) :) 
kilian_air_lightweight_auto_file_watch_minibackup.sh
autominibackups.sh
